// import Tracking from '../src/main'

describe('Tracking', () => {
  test('Tracking', () => {
    // expect(cloned.self === cloned).toBe(true)
    // expect(cloned.children).toEqual([{ children: [] }])
  })
})
